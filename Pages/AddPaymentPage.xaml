<Page x:Class="_122_Zyamilov_Chzhen.Pages.AddPaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавление платежа">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Название -->
        <TextBlock Text="Название платежа:" Grid.Row="0" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <TextBox x:Name="TBPaymentName" Grid.Row="0" Grid.Column="1" Margin="0,10" Height="30"/>

        <!-- Сумма -->
        <TextBlock Text="Сумма:" Grid.Row="1" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <TextBox x:Name="TBAmount" Grid.Row="1" Grid.Column="1" Margin="0,10" Height="30"/>

        <!-- Количество -->
        <TextBlock Text="Количество:" Grid.Row="2" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <TextBox x:Name="TBCount" Grid.Row="2" Grid.Column="1" Margin="0,10" Height="30"/>

        <!-- Дата -->
        <TextBlock Text="Дата платежа:" Grid.Row="3" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <DatePicker x:Name="DPDate" Grid.Row="3" Grid.Column="1" Margin="0,10" Height="30"
                    DisplayDateStart="2020-01-01"
                    DisplayDateEnd="{x:Static sys:DateTime.Now}"/>

        <!-- Клиент -->
        <TextBlock Text="Клиент:" Grid.Row="4" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <ComboBox x:Name="CBUser" Grid.Row="4" Grid.Column="1" Margin="0,10" Height="30"
                  SelectedValuePath="ID" DisplayMemberPath="FIO"/>

        <!-- Категория -->
        <TextBlock Text="Категория:" Grid.Row="5" Grid.Column="0" FontSize="18" Margin="0,10"/>
        <ComboBox x:Name="CBCategory" Grid.Row="5" Grid.Column="1" Margin="0,10" Height="30"
                  SelectedValuePath="ID" DisplayMemberPath="Name"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="ButtonSave" Content="Сохранить" Width="120" Height="40" Margin="10,0" Click="ButtonSave_Click"/>
            <Button x:Name="ButtonClean" Content="Очистить" Width="120" Height="40" Margin="10,0" Click="ButtonClean_Click"/>
        </StackPanel>
    </Grid>
</Page>