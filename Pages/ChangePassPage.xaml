<Page x:Class="_122_Zyamilov_Chzhen.Pages.ChangePassPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChangePassPage">

    <Grid>
        <Viewbox Stretch="Uniform" MaxWidth="800" MaxHeight="500" Margin="0,20,0,20">
            <Border Background="{DynamicResource CenterBackground}" CornerRadius="10" Padding="20">
                <Grid Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                               Text="Смена пароля" FontSize="24" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                               Text="Заполните все поля для смены пароля" FontSize="16"
                               HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Логин" FontSize="14" Margin="0,0,10,5" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="TbLogin" Height="30" Width="300" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Текущий пароль" FontSize="14" Margin="0,0,10,5" VerticalAlignment="Center"/>
                    <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="CurrentPasswordBox" Height="30" Width="300" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Новый пароль" FontSize="14" Margin="0,0,10,5" VerticalAlignment="Center"/>
                    <PasswordBox Grid.Row="4" Grid.Column="1" x:Name="NewPasswordBox" Height="30" Width="300" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Подтверждение пароля" FontSize="14" Margin="0,0,10,5" VerticalAlignment="Center"/>
                    <PasswordBox Grid.Row="5" Grid.Column="1" x:Name="ConfirmPasswordBox" Height="30" Width="300" Margin="0,0,0,10"/>

                    <Button Grid.Row="6" Grid.Column="1" Content="Сохранить"
                            Width="150" Height="30" Margin="0,10,0,0"
                            HorizontalAlignment="Left"
                            Click="SaveButton_Click"/>
                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</Page>
