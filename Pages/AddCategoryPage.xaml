<Page x:Class="_122_Zyamilov_Chzhen.Pages.AddCategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddCategoryPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Название категории" FontSize="20" HorizontalAlignment="Left"/>
            <TextBox x:Name="TBCategoryName" Margin="0,10,0,0" Text="{Binding Name}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button x:Name="ButtonSaveCategory" Content="Сохранить" Click="ButtonSaveCategory_Click" Width="100" Margin="0,0,20,0"/>
            <Button x:Name="ButtonClean" Content="Очистить" Click="ButtonClean_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Page>